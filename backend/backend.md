## Backend starter

`server.js` starts an Express server with a GET endpoint that looks up a Pokémon type through PokéAPI and returns its JSON response:

- **Example URL:** `http://localhost:3000/api/type/fire`
- Replace `fire` with a type name or ID.

Start it from this folder with `npm run dev` (auto-restarts when files change) or `npm start`.
